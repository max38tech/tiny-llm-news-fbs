rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read of articles, but only allow authenticated
    // users to write them.
    match /articles/{articleId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
